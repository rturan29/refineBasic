import { ISession } from 'interfaces';
import React from 'react';

type ApplyWorkshopProps = React.PropsWithChildren<{
    plan: ISession;
}>;

export default function ApplyPlan({ plan }: ApplyWorkshopProps) {
    return (
        <div>

        </div>
    );
}
